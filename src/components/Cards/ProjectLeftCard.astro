---
import TechCard from "./TechCard.astro";
const { tittle, time, text, src } = Astro.props;
---

<article class="grid grid-cols-2 gap-20">
  <figure
    class="bg-white p-5 rounded-2xl grid place-content-center drop-shadow-box"
  >
    <img src={src} alt="" class="rounded-xl" />
  </figure>
  <div class="flex flex-col justify-between">
    <div class="flex flex-col gap-5">
      <h1 class="font-primary text-6xl text-white font-bold">{tittle}</h1>
      <p class="font-primary text-white text-3xl">{text}</p>
    </div>
    <div class="auto-grid">
      <slot />
    </div>
  </div>
</article>
<style>
  .auto-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 32px;
  }
</style>
